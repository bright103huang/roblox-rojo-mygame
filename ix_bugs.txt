修复任务：解决 TaskClient 崩溃并恢复取菜逻辑

1. 报错修复：TaskClient.local.luau 第 128 行报 "attempt to call a nil value"。请检查该行代码，确保所有引用的函数（如绑定触摸事件的函数）已定义，且引用的交互区域（Area）不是 nil。
2. 逻辑核对：
   - 检查 TaskClient 是否成功绑定了 TaskConfig 中定义的 Pickup 区域。
   - 确保当玩家触发 Pickup 区域时，能够正确 FireServer("Pick", "Deliver")。
3. 检查 DeliverTask.lua：确保服务器端的 Pickup 处理逻辑会生成名为 "Dish" 的模型，并正确焊接（Weld）到玩家手上。
4. 容错处理：在 TaskClient 绑定事件前，加入 task.wait(1) 或 WaitForChild，防止因场景加载过慢导致的空指针报错。